# Smart Delivery Zone Planner
**ระบบจัดการพัสดุและจัดเส้นทางจัดส่งอัตโนมัติ**

## 📋 คำอธิบาย

Smart Delivery Zone Planner เป็นระบบจัดการพัสดุและจัดเส้นทางจัดส่งอัตโนมัติ ที่ช่วยในการ:

- **แปลงที่อยู่เป็นพิกัด (Geocoding)** - ใช้ Google Maps API
- **จัดกลุ่มโซนอัตโนมัติ** - จัดกลุ่มตามพื้นที่
- **คำนวณเส้นทางประหยัด** - ใช้ TSP Algorithm
- **แสดงแผนที่และเส้นทาง** - รองรับมือถือ
- **จัดการ Rider** - มอบหมายงานอัตโนมัติ
- **รายงานสรุป** - สถิติการจัดส่ง

## 🛠️ ความต้องการของระบบ

### Server Requirements
- **PHP 7.4+** 
- **MySQL 5.7+** หรือ MariaDB 10.3+
- **Apache/Nginx** Web Server
- **PHP Extensions:**
  - PDO
  - JSON
  - cURL
  - mbstring
  - OpenSSL

### API Keys
- **Google Maps API Key** (จำเป็น)
  - Maps JavaScript API
  - Geocoding API
  - Places API
  - Directions API

## 🚀 การติดตั้ง

### 1. ดาวน์โหลดไฟล์

```bash
# Clone หรือดาวน์โหลดไฟล์โครงการ
git clone https://github.com/your-repo/smart-delivery-zone-planner.git
cd smart-delivery-zone-planner
```

### 2. ตั้งค่าฐานข้อมูล

```bash
# เริ่มต้น MySQL/MariaDB
sudo systemctl start mysql

# สร้างฐานข้อมูล (ถ้าจำเป็น)
mysql -u root -p -e "CREATE DATABASE smart_delivery_db;"
```

### 3. ตั้งค่า Web Server

#### Apache
```apache
<VirtualHost *:80>
    ServerName smart-delivery.local
    DocumentRoot /path/to/smart-delivery-zone-planner
    <Directory /path/to/smart-delivery-zone-planner>
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

#### Nginx
```nginx
server {
    listen 80;
    server_name smart-delivery.local;
    root /path/to/smart-delivery-zone-planner;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
```

### 4. การติดตั้งผ่าน Web Interface

1. เปิดเบราว์เซอร์ไปที่ `http://localhost/imex/install.php`
2. กรอก Google Maps API Key ในไฟล์ `config/config.php`
3. คลิก "เริ่มติดตั้ง"
4. รอจนกว่าการติดตั้งเสร็จสิ้น

### 5. การตั้งค่า Google Maps API Key

แก้ไขไฟล์ `config/config.php`:

```php
// Google Maps API Key
define('GOOGLE_MAPS_API_KEY', 'YOUR_API_KEY_HERE');
```

## 📁 โครงสร้างไฟล์

```
smart-delivery-zone-planner/
├── config/
│   ├── config.php          # การตั้งค่าหลัก
│   └── database.php        # การเชื่อมต่อฐานข้อมูล
├── includes/
│   ├── header.php          # Header ของหน้าเว็บ
│   └── footer.php          # Footer ของหน้าเว็บ
├── pages/
│   ├── import.php          # นำเข้าข้อมูล
│   ├── geocoding.php       # แปลงพิกัด
│   ├── zones.php           # จัดการโซน
│   ├── delivery.php        # จัดการเส้นทาง
│   ├── rider.php           # จัดการ Rider
│   └── reports.php         # รายงาน
├── api/
│   ├── geocoding.php       # API Geocoding
│   ├── zones.php           # API จัดการโซน
│   └── routes.php          # API คำนวณเส้นทาง
├── assets/
│   ├── css/
│   │   └── style.css       # Custom CSS
│   └── js/
│       └── script.js       # Custom JavaScript
├── database/
│   └── schema.sql          # โครงสร้างฐานข้อมูล
├── uploads/                # ไฟล์อัพโหลด
├── index.php               # หน้าหลัก
├── install.php             # ติดตั้งระบบ
└── README.md               # คู่มือนี้
```

## 🗃️ โครงสร้างฐานข้อมูล

### ตารางหลัก

| ตาราง | คำอธิบาย |
|-------|----------|
| `delivery_address` | รายการพัสดุ + พิกัด + โซน |
| `zone_area` | พื้นที่โซน + ขอบเขต |
| `rider` | ข้อมูลพนักงานขนส่ง |
| `delivery_route` | เส้นทางจัดส่งแต่ละวันของ Rider |
| `delivery_history` | ประวัติการจัดส่ง |
| `import_logs` | บันทึกไฟล์นำเข้า |

## 🔧 การใช้งาน

### 1. นำเข้าข้อมูล

1. ไปที่เมนู **นำเข้าข้อมูล**
2. อัพโหลดไฟล์ Excel หรือ CSV
3. ระบบจะอ่านข้อมูลและแปลงที่อยู่เป็นพิกัดอัตโนมัติ

**รูปแบบไฟล์ Excel/CSV:**
```csv
AWB,ชื่อผู้รับ,เบอร์โทร,ที่อยู่,จังหวัด,อำเภอ
TRK001,สมชาย ใจดี,081-234-5678,123 ถนนสุขุมวิท,กรุงเทพฯ,วัฒนา
TRK002,สมหญิง รักดี,082-345-6789,456 ถนนพหลโยธิน,กรุงเทพฯ,จตุจักร
```

### 2. จัดการโซน

1. ระบบจะจัดกลุ่มโซนอัตโนมัติตามพิกัด
2. คุณสามารถปรับแต่งโซนเพิ่มเติมได้
3. กำหนดขอบเขตโซนแต่ละพื้นที่

### 3. คำนวณเส้นทาง

1. เลือกวันที่และ Rider
2. ระบบจะคำนวณเส้นทางที่ประหยัดที่สุด
3. แสดงเส้นทางบนแผนที่พร้อมลำดับการจัดส่ง

### 4. ติดตามการจัดส่ง

1. Rider สามารถเปิดแผนที่บนมือถือ
2. ติดตามสถานะการจัดส่งแบบ Real-time
3. บันทึกหลักฐานการจัดส่ง

## 📊 ฟีเจอร์หลัก

### 🌍 Geocoding
- แปลงที่อยู่เป็นพิกัด (Latitude, Longitude)
- ใช้ Google Maps API
- ตรวจสอบความถูกต้องของที่อยู่

### 🗺️ Zone Management
- จัดกลุ่มโซนอัตโนมัติ
- กำหนดขอบเขตพื้นที่
- แสดงโซนบนแผนที่

### 🚚 Route Optimization
- คำนวณเส้นทางประหยัด (TSP)
- ประมาณเวลาการจัดส่ง
- รองรับหลาย Rider

### 📱 Mobile Responsive
- ใช้งานบนมือถือได้
- แผนที่แบบ Touch-friendly
- อินเทอร์เฟซที่เป็นมิตรกับผู้ใช้

### 📈 Reports & Analytics
- รายงานการจัดส่งตามโซน
- สถิติประสิทธิภาพ Rider
- ติดตามสถานะแบบ Real-time

## 🔐 ความปลอดภัย

- **Input Validation** - ตรวจสอบข้อมูลนำเข้า
- **SQL Injection Protection** - ใช้ PDO Prepared Statements
- **XSS Protection** - ป้องกันการโจมตี Cross-Site Scripting
- **File Upload Security** - จำกัดประเภทไฟล์ที่อัพโหลด

## 🛠️ การปรับแต่ง

### เปลี่ยนการตั้งค่าฐานข้อมูล

แก้ไขไฟล์ `config/database.php`:

```php
private $host = 'localhost';
private $db_name = 'smart_delivery_db';
private $username = 'root';
private $password = '';
```

### เปลี่ยนการตั้งค่าโซน

แก้ไขไฟล์ `config/config.php`:

```php
// Default zone radius in kilometers
define('DEFAULT_ZONE_RADIUS', 5);

// Maximum deliveries per route
define('MAX_DELIVERIES_PER_ROUTE', 50);
```

## 🐛 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย

#### 1. Google Maps API Key ไม่ทำงาน
- ตรวจสอบ API Key ใน `config/config.php`
- เปิดใช้งาน APIs ที่จำเป็น
- ตรวจสอบ Quota และ Billing

#### 2. ฐานข้อมูลเชื่อมต่อไม่ได้
- ตรวจสอบ MySQL/MariaDB Service
- ตรวจสอบ Username/Password
- ตรวจสอบ Port และ Host

#### 3. ไฟล์อัพโหลดไม่ได้
- ตรวจสอบ PHP `upload_max_filesize`
- ตรวจสอบ PHP `post_max_size`
- ตรวจสอบสิทธิ์ของโฟลเดอร์ `uploads/`

## 🤝 การสนับสนุน

หากต้องการความช่วยเหลือ:

1. ตรวจสอบ Issues ใน GitHub
2. อ่าน Documentation
3. สร้าง Issue ใหม่พร้อมรายละเอียด

## 📄 License

MIT License - ดูไฟล์ `LICENSE` สำหรับรายละเอียด

## 🔄 Changelog

### Version 1.0.0
- ระบบนำเข้าข้อมูล
- ระบบ Geocoding  
- ระบบจัดกลุ่มโซน
- ระบบคำนวณเส้นทาง
- ระบบจัดการ Rider
- ระบบรายงาน
- Mobile Responsive UI

---

**Smart Delivery Zone Planner** - ระบบจัดการพัสดุและจัดเส้นทางจัดส่งอัตโนมัติ
© 2024 All Rights Reserved 